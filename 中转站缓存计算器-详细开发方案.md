# 中转站缓存成本计算器——详细开发方案

> **文档状态：历史设计记录。** 本文保留早期产品与技术决策，部分接口和安全边界已被后续版本替代。当前行为以 [`README.md`](./README.md)、[`SECURITY.md`](./SECURITY.md) 和源码为准。

> 文档版本：v1.1  
> 初版日期：2026-08-23  
> 小白模式修订：2026-08-24  
> 项目类型：React 单页计算工具 + Cloudflare Pages Function  
> 默认语言：简体中文  
> 默认结算货币：人民币（CNY）

## 1. 项目概述

### 1.1 项目背景

不同 AI API 中转站通常会在模型基础价格之上设置模型倍率、渠道倍率或分组倍率。同时，支持 Prompt Cache 的模型还会将普通输入、缓存读取、缓存写入和模型输出按照不同价格计费。

用户从中转站监控页面获得模型倍率和缓存命中率后，很难快速判断：

- 每 1M 输入 token 实际需要花费多少钱；
- 每 1M 输入与输出混合 token 实际需要花费多少钱；
- 花费 10 元或其他指定预算可以使用多少 token；
- 缓存命中实际节省了多少钱；
- 中转站账单是否与模型单价、倍率和缓存数据相符。

本项目拟开发一个“中转站缓存成本计算器”，通过模型基础价格、中转站倍率、缓存命中率和输入输出结构，实时计算实际使用成本及预算可用量。产品同时提供独立“小白模式”：用户输入中转站 Base URL 后，由同源 Cloudflare Pages Function 尝试读取 New API 兼容配置；用户也可以临时提供普通中转 API Key，以从最近日志中估算运行时缓存命中率。

### 1.2 核心结论

仅有“模型倍率”和“缓存率”无法独立计算人民币成本。计算器还必须知道以下信息之一：

1. 模型的普通输入、缓存读取和输出基础单价；或
2. 中转站已经换算完成的最终单价。

为了让普通用户仍然只需重点填写“模型倍率”和“缓存率”，产品应提供模型价格预设；选择模型后自动带出基础单价，同时允许高级用户覆盖价格或选择“自定义模型”。小白模式可优先使用中转站暴露的倍率配置，并在接口不可用时明确降级到手填。

必须区分两个名称相似但用途完全不同的量：

- `cache_ratio`：缓存读取的**计费价格倍率**，用于推导缓存读取单价；
- 运行时缓存命中率：最近调用中缓存 token 所占比例，例如界面上的“缓存命中 76.76%”，用于估算有多少输入按缓存价计费。

计费缓存倍率不能当作运行时缓存命中率，缓存命中率也不能代替缓存单价。

## 2. 项目目标与范围

### 2.1 MVP 目标

- 支持模型预设和自定义模型价格；
- 支持人民币和美元基础价格；
- 支持模型倍率及可选渠道/分组倍率；
- 支持缓存命中率输入；
- 支持仅输入 token、混合 token、精确用量三种计算模式；
- 计算每 1M token 实际成本；
- 计算指定人民币预算可使用的 token 数量；
- 显示缓存节省金额和节省比例；
- 显示费用构成和完整代入公式；
- 本地保存用户上次使用的设置；
- 支持桌面端和移动端；
- 新增与原简易/高级模式隔离的“小白模式”；
- 小白模式支持输入 Base URL，自动读取可公开访问的 New API 兼容倍率与分组配置；
- 可选使用普通中转 API Key 读取 `/api/log/token` 最近日志并估算缓存命中率；
- 所有金额计算仍在浏览器本地完成；只有小白模式的 Base URL 和可选 API Key 会经同源 Pages Function 转发。

### 2.2 MVP 不包含

- 用户账号及云端同步；
- 自动抓取各模型最新官方价格；
- 自动读取完整真实账单或无限期用量历史；
- 使用面板管理员凭据、用户面板 Cookie 或管理员接口；
- 绕过运营方的 HTTP 451、地域限制、登录限制或关闭的公开接口；
- 对所有 One API / New API 私有 fork 保证兼容；
- 多用户协作；
- 支付、充值或交易功能；
- 后端数据库。

### 2.3 后续可扩展能力

- 多模型成本横向对比；
- 多个中转站方案对比；
- 账单历史与趋势分析；
- 从更多 API usage JSON 自动导入用量；
- 经审核的中转站域名 allowlist；
- 可分享的 URL 参数；
- 远程模型价格表；
- 汇率自动更新；
- 导出 CSV、图片或 PDF 报告；
- PWA 和离线使用。

## 3. 用户与典型使用场景

### 3.1 目标用户

- 使用 AI API 中转站的个人用户；
- 需要评估模型调用成本的开发者；
- 管理多个模型和渠道的中转站运营者；
- 需要核对缓存账单的技术或财务人员。

### 3.2 核心使用场景

#### 场景 A：快速估算输入缓存成本

用户选择模型，填写模型倍率和缓存命中率，直接查看每 1M 输入 token 的实际价格以及 10 元可用的输入 token 数量。

#### 场景 B：估算完整对话成本

用户设置输入与输出比例，例如 4:1，计算输入和输出合计 1M token 的实际成本。

#### 场景 C：核对真实调用账单

用户直接填写普通输入 token、缓存读取 token、缓存写入 token 和输出 token，计算该次调用的理论费用。

#### 场景 D：比较缓存是否值得

用户调整缓存命中率，查看成本变化、节省金额和节省比例。

#### 场景 E：小白模式自动读取

用户只知道中转站 Base URL。工具尝试读取模型倍率、分组倍率和计费缓存倍率；如果用户愿意临时提供低权限普通中转 API Key，再从最近日志中估算对应模型/分组的运行时缓存命中率。任一数据源不可用时，保留已读取的部分并提示用户手填缺失项，不影响简易模式和高级模式。

## 4. 关键计费口径

### 4.1 统一单位

- 所有基础单价统一表示为“每 1M token 的价格”；
- 内部 token 基准值固定为 `1,000,000`；
- 最终金额统一换算为人民币；
- 中间计算不得提前四舍五入；
- 页面展示时再进行金额和 token 格式化。

### 4.2 变量定义

| 变量 | 字段名建议 | 含义 |
|---|---|---|
| `Pi` | `inputPricePerMillion` | 普通输入单价 |
| `Pc` | `cachedReadPricePerMillion` | 缓存读取单价 |
| `Pw` | `cacheWritePricePerMillion` | 缓存写入单价，可选 |
| `Po` | `outputPricePerMillion` | 输出单价 |
| `M` | `modelMultiplier` | 中转站模型倍率 |
| `G` | `groupMultiplier` | 渠道或分组倍率，默认 1 |
| `X` | `exchangeRateToCny` | 单价币种兑人民币换算率 |
| `H` | `cacheHitRate` | 缓存命中率，内部使用 0～1 |
| `Kc` | `cacheRatio` | 缓存读取计费倍率（New API 的 `cache_ratio`） |
| `Kw` | `createCacheRatio` | 缓存写入计费倍率（New API 的 `create_cache_ratio`，可选） |
| `Q` | `outputShare` | 输出 token 占总 token 的比例 |
| `B` | `budgetCny` | 人民币预算，默认 10 元 |

### 4.3 缓存命中率默认定义

MVP 默认使用以下口径：

```text
缓存命中率 = 缓存读取 token ÷ 全部输入 token
```

页面必须在缓存率输入框附近显示口径说明，不能只显示“缓存率”三个字。

页面还必须把 `cache_ratio` 标注为“计费缓存倍率”或“缓存读取价格倍率”，把 `H` 标注为“运行时缓存命中率”。如果普通输入基准单价为 `Pi`，则 New API 计费倍率对应的缓存读取价为：

```text
Pc = Pi × Kc
```

该公式只决定命中缓存的 token 按什么价格计费，不说明实际有多少 token 命中缓存。

高级设置可增加另一种口径：

```text
缓存命中率 = 缓存读取 token ÷ 输入与输出总 token
```

当缓存率以总 token 为分母时，应先换算为输入 token 内部的缓存占比：

```text
输入占比 = 1 - 输出占比
输入内缓存占比 = 总 token 缓存率 ÷ 输入占比
```

此时必须验证“总 token 缓存率不能大于输入占比”。

### 4.4 倍率应用规则

支持两种计价方式：

#### 基础单价 × 倍率

适用于用户输入官方价格或未加倍率的基准价格：

```text
实际倍率 = 模型倍率 × 渠道/分组倍率
```

#### 直接输入站内最终单价

适用于用户输入的价格已经包含全部中转倍率。该模式下不再乘模型倍率或分组倍率，避免重复计算。

界面上应使用明确的计价方式选择，而不是仅提供一个容易忽略的复选框。

## 5. 计算模式与公式

### 5.1 模式一：每 1M 输入 token

这是默认模式，也是最适合观察缓存效果的模式。

有效输入单价：

```text
Peffective = Pi × (1 - H) + Pc × H
```

人民币实际成本：

```text
Cinput = Peffective × M × G × X
```

如果选择“最终单价模式”，则：

```text
Cinput = Peffective × X
```

指定预算可使用的输入 token：

```text
TokensForBudget = B ÷ Cinput × 1,000,000
```

### 5.2 模式二：每 1M 混合 token

“混合 token”表示输入和输出合计为 1M token。

```text
输入占比 = 1 - Q
输出占比 = Q
```

每 1M 混合 token 成本：

```text
Cmixed = [(1 - Q) × Peffective + Q × Po] × M × G × X
```

最终单价模式下不再乘 `M × G`。

预算可用总 token：

```text
TotalTokensForBudget = B ÷ Cmixed × 1,000,000
```

预算用量拆分：

```text
InputTokensForBudget = TotalTokensForBudget × (1 - Q)
OutputTokensForBudget = TotalTokensForBudget × Q
CachedTokensForBudget = InputTokensForBudget × H
NormalInputTokensForBudget = InputTokensForBudget × (1 - H)
```

### 5.3 模式三：精确用量账单

为避免不同 API 对 `input_tokens` 的包含关系定义不同，本模式不要求用户填写“总输入 token”，而是直接填写互斥的费用桶：

- 普通输入 token；
- 缓存读取 token；
- 缓存写入 token；
- 输出 token。

计算公式：

```text
Cexact = (
  NormalInputTokens × Pi
  + CachedReadTokens × Pc
  + CacheWriteTokens × Pw
  + OutputTokens × Po
) ÷ 1,000,000 × M × G × X
```

最终单价模式下不再乘 `M × G`。

如果模型不收取缓存写入费，可以隐藏 `Pw` 和缓存写入 token 字段，或将其置为 0。

### 5.4 无缓存成本与缓存节省

混合模式下的无缓存成本：

```text
CnoCache = [(1 - Q) × Pi + Q × Po] × M × G × X
```

缓存节省金额：

```text
Savings = CnoCache - Cmixed
```

缓存节省比例：

```text
SavingsRate = Savings ÷ CnoCache × 100%
```

当 `CnoCache = 0` 时，不计算节省比例，页面显示“无法计算”。

### 5.5 缓存折扣系数模式

部分用户只知道缓存价格是普通输入价格的某个比例，例如 0.1 倍。可以提供以下两种缓存价格输入方式：

1. 直接输入缓存读取单价；
2. 输入缓存价格系数 `K`。

系数模式公式：

```text
Pc = Pi × K
```

模型预设应优先保存明确的缓存读取单价；系数模式主要用于自定义模型。

## 6. 标准计算示例

### 6.1 示例输入

| 参数 | 数值 |
|---|---:|
| 普通输入价格 | 10 元/1M |
| 缓存读取价格 | 1 元/1M |
| 输出价格 | 30 元/1M |
| 模型倍率 | 1.2 |
| 分组倍率 | 1 |
| 缓存命中率 | 60% |
| 输入占比 | 80% |
| 输出占比 | 20% |
| 预算 | 10 元 |

### 6.2 有效输入单价

```text
10 × 40% + 1 × 60%
= 4 + 0.6
= 4.6 元/1M
```

倍率后的每 1M 输入 token 成本：

```text
4.6 × 1.2 = 5.52 元
```

### 6.3 每 1M 混合 token 成本

```text
(80% × 4.6 + 20% × 30) × 1.2
= (3.68 + 6) × 1.2
= 11.616 元
```

### 6.4 10 元可使用的混合 token

```text
10 ÷ 11.616 × 1,000,000
≈ 860,881.54 token
```

页面整数展示可四舍五入为：

```text
860,882 token
```

### 6.5 缓存节省

无缓存成本：

```text
(80% × 10 + 20% × 30) × 1.2
= 16.8 元
```

节省金额：

```text
16.8 - 11.616 = 5.184 元
```

节省比例：

```text
5.184 ÷ 16.8 ≈ 30.86%
```

该示例必须作为计算引擎的固定单元测试和产品验收样例。

## 7. 功能需求

### 7.1 模型价格配置

- 支持从本地价格表选择模型；
- 自动填充模型提供商、币种和各类单价；
- 显示价格更新时间；
- 可选显示价格来源链接；
- 允许覆盖预设价格；
- 支持新增临时自定义模型；
- 支持人民币和美元；
- 美元价格必须提供兑人民币换算率；
- 支持缓存读取单价或缓存价格系数两种方式。

### 7.2 中转站配置

- 模型倍率，默认 1；
- 渠道或分组倍率，默认 1，放在高级设置；
- 基础单价模式与最终单价模式切换；
- 计价方式变化后实时重新计算；
- 防止最终单价被重复乘倍率。

### 7.3 使用结构配置

- 缓存命中率数字输入；
- 缓存命中率滑块；
- 数字输入与滑块双向同步；
- 缓存率口径选择；
- 计算模式切换；
- 混合模式下设置输入和输出比例；
- 支持“输入:输出”的直观形式，例如 4:1；
- 支持预算金额输入，默认 10 元。

### 7.4 结果展示

- 每 1M 输入 token 实际成本；
- 每 1M 输出 token 实际成本；
- 每 1M 混合 token 实际成本；
- 指定预算可用总 token；
- 预算内普通输入、缓存输入和输出 token 拆分；
- 无缓存成本；
- 缓存节省金额；
- 缓存节省比例；
- 普通输入、缓存读取和输出成本构成；
- 完整计算公式；
- 实际数字代入过程；
- 异常口径或异常价格提醒。

### 7.5 辅助功能

- 一键重置；
- 一键复制结果摘要；
- 自动保存设置；
- 支持恢复默认值；
- 页面首次打开显示可理解的示例状态；
- 不使用空白页面或无法理解的零数据状态。

### 7.6 独立小白模式

#### 7.6.1 模式隔离

- 顶部模式选择为“小白模式 / 简易模式 / 高级模式”；
- 小白模式使用独立的组件状态和计算适配层，不把自动读取结果写入原 `CalculatorSettings`；
- 进入、退出或重置小白模式，不修改简易/高级模式已经保存的模型、单价、倍率、缓存率、比例或预算；
- 简易与高级模式继续使用原计算引擎、持久化键和交互，不因小白模式接口失败而显示全局错误；
- 小白模式完成数据标准化后可以复用结果、费用构成和公式展示组件，但不能让远端原始 JSON 直接进入计算引擎。

#### 7.6.2 用户输入

必填：

- 中转站 Base URL，只允许 HTTPS；
- 自动读取完成后的模型和分组选择。

可选：

- 普通中转 API Key，仅用于本次 `/api/log/token` 最近日志读取；
- 无法自动获得时手填运行时缓存命中率；
- 汇率和预算等小白模式允许调整的估算参数。

API Key 输入附近必须显示：不会保存、会发送给本站同源 Pages Function、只用于读取最近日志、请求结束后清空。不得诱导用户输入管理员 Key、面板账号密码、Cookie 或高权限 Token。

#### 7.6.3 New API 兼容端点

Pages Function 只尝试 Base URL 下的固定路径：

| 路径 | 用途 | 认证策略 |
|---|---|---|
| `/api/status` | 识别站点、版本与基本可达性 | 不发送用户 Key |
| `/api/pricing` | 模型、倍率、计费缓存倍率与可用分组 | 不发送用户 Key；若站点要求登录则记录不可用 |
| `/api/ratio` | 兼容部分私有 fork 的倍率配置 | 不发送用户 Key |
| `/api/ratio_config` | 读取公开倍率映射 | 不发送用户 Key |
| `/api/user/groups` | 读取公开可用分组与分组倍率 | 不发送用户 Key |
| `/api/rankings` | 兼容提供公开监控/缓存统计的站点 | 不发送用户 Key |
| `/api/log/token` | 读取普通用户最近日志并估算缓存命中率 | 仅此路径携带本次普通中转 API Key |

端点返回格式因版本和 fork 而异。解析器应采用白名单字段、数值范围检查和分段容错：一个端点失败不应抹掉其他端点已读取的数据。对于标准 New API，可组合 `model_ratio`、`completion_ratio`、`cache_ratio`、`create_cache_ratio`、`group_ratio` 等字段；私有 fork 的 `/api/ratio` 和 `/api/rankings` 只做兼容增强，不能成为唯一数据源。

#### 7.6.4 缓存命中率来源

优先级建议如下：

1. 用户提供 API Key 时，从 `/api/log/token` 最近日志按模型和分组聚合；
2. 站点 `/api/rankings` 明确返回可识别的缓存 token / 命中率字段时使用公开监控值；
3. 无可用数据时让用户手填，并清楚标注来源为“手动”。

OpenAI 语义通常按 `cached_tokens ÷ prompt_tokens`；Anthropic Messages 的某些日志把普通输入和缓存输入拆开，应按 `cached_tokens ÷ (prompt_tokens + cached_tokens)`。解析器必须根据可确认的协议/日志字段选择分母；无法确认时不能假装精确，应提示用户核对口径。

展示层必须显示缓存命中率的来源、样本条数和时间范围（能获得时）。最近日志只是样本估算，不等于长期平均值，也不等于未来请求必然命中。

#### 7.6.5 降级与错误提示

- 公开倍率接口关闭、要求面板登录或返回未知格式：保留已读取项，缺失项转为手填；
- 旧 One API 没有兼容端点：提示“该站暂不支持自动读取”，可继续使用简易/高级模式；
- `/api/log/token` 返回 401/403：提示普通 API Key 无效或站点不允许读取日志，并立即清空 Key；
- 上游返回 HTTP 451：提示目标站受限并停止，不切换备用域名、不借助其他代理、不重试绕过；
- 超时、响应过大或网络失败：显示端点级状态，允许重试，但不暴露原始响应中的敏感内容；
- 自动结果缺少模型倍率、分组倍率、输出倍率或缓存计费倍率时，不输出貌似完整的成本结论。

## 8. 页面信息架构

### 8.1 页面总体结构

本项目采用单页面工具结构，不增加营销首页和复杂导航。顶部先选择“小白模式 / 简易模式 / 高级模式”；下方输入区与结果区随模式切换，但结果展示语言和金额口径保持一致。

```text
┌─────────────────────────────────────────────────────────┐
│ 标题、说明、重置、复制结果                              │
├────────────────────────┬────────────────────────────────┤
│ 计价参数               │ 实时结果                       │
│                        │                                │
│ 1. 模型价格            │ 1. 每 1M 输入成本              │
│ 2. 中转站倍率          │ 2. 每 1M 混合成本              │
│ 3. 缓存率              │ 3. 预算可用 token              │
│ 4. 输入输出结构        │ 4. 缓存节省                    │
│ 5. 预算                │ 5. 费用构成                    │
│ 6. 高级设置            │ 6. 公式详情                    │
└────────────────────────┴────────────────────────────────┘
```

### 8.2 顶部区域

建议可见文案：

- 标题：`中转站缓存成本计算器`
- 说明：`输入模型单价、倍率与缓存命中率，换算每 1M token 成本和预算可用量。`
- 操作：`重置`、`复制结果`

不增加装饰性的徽章、统计数字或无关导航。

### 8.3 左侧输入区

按以下顺序排列：

1. 计价方式；
2. 模型与基础价格；
3. 中转站倍率；
4. 计算模式；
5. 缓存命中率；
6. 输入输出结构；
7. 预算金额；
8. 高级设置。

高级设置默认折叠，包含：

- 渠道/分组倍率；
- 缓存率分母口径；
- 缓存价格系数模式；
- 缓存写入价格；
- 显示精度。

### 8.4 右侧结果区

第一视觉层级使用一个主结果面板，不采用大量同级小卡片。

建议结果顺序：

1. 当前模式的每 1M token 主结果；
2. 指定预算可用 token；
3. 输入、缓存和输出拆分；
4. 缓存节省；
5. 费用构成条；
6. 可展开的公式详情。

桌面端结果区可设置为吸顶，用户修改左侧参数时结果始终可见。

### 8.5 移动端

- 输入区与结果区上下排列；
- 主结果放在输入区之后；
- 所有输入控件宽度为 100%；
- 输入输出比例控件允许换行；
- 不出现横向滚动；
- 可在底部显示不遮挡内容的紧凑摘要：

```text
¥11.616/1M · 10元≈860.9K
```

### 8.6 小白模式页面结构

```text
┌─────────────────────────────────────────────────────────┐
│ 小白模式 | 简易模式 | 高级模式                          │
├────────────────────────┬────────────────────────────────┤
│ 1. Base URL            │ 读取状态与来源                  │
│ 2. 可选普通 API Key    │ 模型/分组/倍率                  │
│ 3. 开始读取             │ 缓存命中率与样本               │
│ 4. 模型与分组选择       │ 成本、预算、构成、公式          │
│ 5. 缺失项手填           │                                │
└────────────────────────┴────────────────────────────────┘
```

界面要把“读取配置”和“读取最近日志”说明为两类动作。即使在同一次请求中并行完成，也必须让用户知道可选 API Key 只影响日志读取，公开配置读取不需要、也不应携带该 Key。

## 9. 视觉与交互规范

### 9.1 视觉方向

- 风格：专业、轻量、可信赖的计算工具；
- 背景：真白或明确的浅灰背景，不使用复杂纹理；
- 主文字：深灰蓝；
- 强调色：青绿色，用于主结果和缓存节省；
- 警告色：琥珀色；
- 错误色：红色；
- 圆角：约 10～12px；
- 阴影：轻微或不使用；
- 边框：低对比度中性边框；
- 数值：启用等宽数字特性；
- 不使用复杂渐变、霓虹光效和无意义插图。

### 9.2 字体层级

- 页面标题：28～32px；
- 主结果数字：40～48px；
- 区域标题：16～18px；
- 正文与输入：14～16px；
- 辅助说明：12～13px；
- 中文优先使用系统字体栈；
- 数值使用 `font-variant-numeric: tabular-nums`。

### 9.3 交互规则

- 输入变化后立即计算；
- 空字段暂不计算，不显示 `NaN`；
- 滑块和数字框保持同步；
- 模式切换只显示当前模式需要的字段；
- 公式详情默认折叠；
- 复制成功显示轻量提示；
- 重置操作需要明确，但不必弹出阻塞式确认框；
- 动画时长建议 150～200ms；
- 尊重 `prefers-reduced-motion`。

## 10. 技术方案

### 10.1 推荐技术栈

- React；
- TypeScript；
- Vite；
- CSS Variables 或轻量模块化 CSS；
- 十进制高精度计算方案；
- 单元测试框架；
- 浏览器端端到端测试工具。

实现时使用当时的稳定版本，不在业务代码中依赖实验性 API。

### 10.2 架构原则

- 计算引擎与 UI 完全解耦；
- 计算函数必须是纯函数；
- 不在组件中直接散落价格公式；
- 所有单位转换集中处理；
- 所有格式化集中处理；
- UI 只负责收集参数和展示结果；
- 小白模式的网络适配、远端格式解析和核心计算分层，远端响应不能直接驱动计算；
- Pages Function 只负责受限读取与标准化，不保存用户数据、不承担金额计算；
- 模型价格数据与代码分离；
- MVP 不引入大型全局状态管理库；
- 不引入仅用于一个简单构成条的图表库。

### 10.3 建议目录结构

```text
缓存计算器/
├── public/
├── functions/
│   ├── api/
│   │   └── relay/
│   │       └── inspect.ts
│   └── _lib/
│       └── relay-inspect.ts
├── src/
│   ├── app/
│   │   └── App.tsx
│   ├── components/
│   │   ├── NumberField.tsx
│   │   ├── PercentField.tsx
│   │   ├── SegmentedControl.tsx
│   │   ├── ResultValue.tsx
│   │   └── InlineNotice.tsx
│   ├── features/
│   │   ├── calculator/
│   │   │   ├── CalculatorForm.tsx
│   │   │   ├── PricingSection.tsx
│   │   │   ├── RelaySection.tsx
│   │   │   ├── WorkloadSection.tsx
│   │   │   ├── ResultSummary.tsx
│   │   │   ├── CostBreakdown.tsx
│   │   │   ├── FormulaDetails.tsx
│   │   │   ├── calculator.engine.ts
│   │   │   ├── calculator.types.ts
│   │   │   ├── calculator.validation.ts
│   │   │   └── calculator.test.ts
│   │   └── novice/
│   │       ├── NoviceMode.tsx
│   │       ├── relay.client.ts
│   │       ├── relay.types.ts
│   │       └── useNoviceCalculator.ts
│   ├── data/
│   │   └── models.json
│   ├── hooks/
│   │   └── usePersistedSettings.ts
│   ├── styles/
│   │   ├── tokens.css
│   │   └── global.css
│   ├── utils/
│   │   ├── decimal.ts
│   │   └── format.ts
│   ├── main.tsx
│   └── vite-env.d.ts
├── tests/
│   └── calculator.e2e.ts
├── index.html
├── package.json
├── wrangler.jsonc
├── tsconfig.functions.json
├── tsconfig.json
└── vite.config.ts
```

`functions/` 和 `wrangler.jsonc` 必须位于项目根。Cloudflare Dashboard 拖拽 Direct Upload 不会包含 Pages Functions；本项目必须从根目录使用 Wrangler CLI 部署。

### 10.4 状态管理

MVP 使用组件状态或 `useReducer` 即可。

状态分为四类：

1. 用户输入状态；
2. 由输入派生的计算结果；
3. 展示状态，例如高级设置是否展开；
4. 小白模式临时状态，例如 Base URL、读取状态、端点结果和只存在于本次请求内存的 API Key。

计算结果不得重复保存到持久化状态，应通过计算引擎实时派生，避免输入与结果不一致。

小白模式的临时状态不进入原简易/高级设置对象。API Key 不得进入 reducer 快照、持久化 hook、URL、调试日志或错误上报；请求完成（成功或失败）后必须清空。

### 10.5 Pages Function 安全边界

Function 是一个受限的同源读取代理，而不是通用 URL 抓取器。最小安全要求：

- Base URL 只允许 `https:`，拒绝 URL 中的用户名/密码、query、fragment、非 443 端口和非根路径（可规范化 `/v1`）；
- 拒绝 IP 字面量、localhost、私网、链路本地、回环、保留地址和常见内部域名后缀；
- 请求前进行 A/AAAA DNS 预检，并启用 Wrangler `global_fetch_strictly_public`；
- 只能拼接第 7.6.3 节列出的固定路径，不接受用户传入的 endpoint、method 或 header；
- 手动处理或拒绝上游重定向，防止公开域名跳转到内网；
- 对请求超时、响应状态、Content-Type、最大响应体和 JSON 结构设置限制；
- API Key 只在 `/api/log/token` 请求中作为普通中转认证头使用，其他端点绝不携带；
- 不记录请求体、Authorization、完整上游响应或可能包含凭据的异常；响应设置 `Cache-Control: no-store`；
- 目标站返回 451 时直接返回受限状态，不尝试绕过；
- Function 只接受本站同源前端调用，并配合速率限制/WAF 规则控制滥用（如账户能力允许）。

残余风险必须写进产品与运维文档：允许任意公网域名不是 allowlist 的绝对保证。DNS 预检与实际连接之间仍可能发生 DNS rebinding 或解析变化，合法公网主机也可能返回恶意、超大或消耗资源的数据。`global_fetch_strictly_public`、DNS 预检、固定路径和响应限制共同降低风险，但不能证明目标可信。更严格的后续方案是运营方审核域名 allowlist、专用出站代理或把自动读取限定到明确支持的中转站。

## 11. 数据模型

### 11.1 类型定义建议

```ts
type Currency = 'CNY' | 'USD'

type PricingMode =
  | 'base-times-multiplier'
  | 'final-unit-price'

type ScenarioMode =
  | 'input-only'
  | 'mixed-total'
  | 'exact-usage'

type CacheRateBasis =
  | 'input-tokens'
  | 'total-tokens'

interface ModelPrice {
  id: string
  name: string
  provider: string
  currency: Currency
  inputPerMillion: string
  cachedReadPerMillion: string
  outputPerMillion: string
  cacheWritePerMillion?: string
  updatedAt: string
  sourceUrl?: string
}

interface RelaySettings {
  pricingMode: PricingMode
  modelMultiplier: string
  groupMultiplier: string
  exchangeRateToCny: string
}

interface WorkloadSettings {
  scenarioMode: ScenarioMode
  cacheHitRatePercent: string
  cacheRateBasis: CacheRateBasis
  inputRatio: string
  outputRatio: string
  budgetCny: string
}

interface ExactUsage {
  normalInputTokens: string
  cachedReadTokens: string
  cacheWriteTokens: string
  outputTokens: string
}
```

表单层建议保留字符串，避免用户输入小数点过程中被强制转换；进入计算引擎前再进行解析和校验。

### 11.2 结果模型建议

```ts
interface CalculationResult {
  effectiveInputUnitPrice: string
  inputCostPerMillionCny: string
  outputCostPerMillionCny: string
  mixedCostPerMillionCny?: string
  exactUsageCostCny?: string
  noCacheCostPerMillionCny?: string
  savingsCny?: string
  savingsPercent?: string
  budgetCapacity?: {
    totalTokens: string
    normalInputTokens: string
    cachedInputTokens: string
    outputTokens: string
  }
  breakdown: {
    normalInputCostCny: string
    cachedReadCostCny: string
    cacheWriteCostCny: string
    outputCostCny: string
  }
  warnings: string[]
}
```

金额和 token 在核心层可以使用高精度对象，跨模块输出时再统一序列化为字符串。

### 11.3 小白模式标准化模型

```ts
type RelayDataSource =
  | 'pricing'
  | 'ratio'
  | 'ratio_config'
  | 'groups'
  | 'rankings'
  | 'logs'
  | 'manual'

interface RelayModel {
  name: string
  modelRatio: string | null
  completionRatio: string | null
  cacheRatio: string | null
  createCacheRatio: string | null
  enabledGroups: string[]
  sources: RelayDataSource[]
}

interface RelayGroup {
  id: string
  name: string
  ratio: string | null
  source: RelayDataSource
}

interface RelayCacheStat {
  modelName: string
  group: string | null
  hitRatePercent: string
  sampleCount: number
  fromTimestamp?: number
  toTimestamp?: number
  source: 'rankings' | 'logs' | 'manual'
}
```

所有倍率和百分比继续以十进制字符串跨模块传递。标准化响应应附带每个端点的成功/失败状态和可读警告，但不得包含 API Key、Authorization、日志原文或无关个人数据。

## 12. 计算引擎设计

### 12.1 主函数

```ts
function calculateCost(input: CalculatorInput): CalculationResult
```

主函数内部流程：

1. 解析输入；
2. 执行字段校验；
3. 统一币种；
4. 计算实际倍率；
5. 标准化缓存率口径；
6. 计算有效输入单价；
7. 根据模式计算成本；
8. 计算预算容量；
9. 计算无缓存成本与节省；
10. 生成费用构成；
11. 生成警告；
12. 返回未经展示层舍入的结果。

### 12.2 伪代码

```ts
const fx = currency === 'CNY' ? 1 : exchangeRateToCny

const multiplier = pricingMode === 'final-unit-price'
  ? 1
  : modelMultiplier * groupMultiplier

const inputShare = inputRatio / (inputRatio + outputRatio)
const outputShare = outputRatio / (inputRatio + outputRatio)

const cacheShareOfInput = cacheRateBasis === 'input-tokens'
  ? cacheHitRate
  : cacheHitRate / inputShare

const effectiveInputPrice =
  inputPrice * (1 - cacheShareOfInput)
  + cachedReadPrice * cacheShareOfInput

const inputCostPerMillionCny =
  effectiveInputPrice * multiplier * fx

const mixedCostPerMillionCny =
  (
    inputShare * effectiveInputPrice
    + outputShare * outputPrice
  ) * multiplier * fx

const totalTokensForBudget = mixedCostPerMillionCny > 0
  ? budgetCny / mixedCostPerMillionCny * 1_000_000
  : null
```

正式代码使用高精度十进制运算，不能直接依赖二进制浮点数完成最终金额计算。

### 12.3 格式化规则

- 金额默认显示 4 位小数；
- 小于 `0.0001` 元的非零金额可扩展到 6 位；
- token 完整数值显示千位分隔符；
- 辅助显示使用 `K`、`M`、`B`；
- 百分比默认显示 2 位小数；
- 中间计算不舍入；
- 复制结果时同时保留完整值和便于阅读的简写。

## 13. 模型价格预设

### 13.1 本地数据文件

模型价格保存到 `src/data/models.json`。

```json
[
  {
    "id": "custom-example",
    "name": "示例模型",
    "provider": "示例提供商",
    "currency": "CNY",
    "inputPerMillion": "10",
    "cachedReadPerMillion": "1",
    "outputPerMillion": "30",
    "updatedAt": "2026-08-23"
  }
]
```

正式发布前应将示例价格与真实模型预设严格区分，不能让用户误以为示例价格是当前官方价格。

### 13.2 价格更新原则

- 每条预设包含更新时间；
- 有可靠来源时保存来源链接；
- 页面提示预设价格可能变化；
- 用户可以覆盖任何预设字段；
- MVP 不在前端静默抓取第三方价格；
- 未来接入远程价格表时保留本地回退数据。

## 14. 本地持久化

### 14.1 保存范围

建议保存：

- 最近选择的模型；
- 自定义单价；
- 倍率；
- 缓存率；
- 输入输出比例；
- 预算；
- 计价方式；
- 高级设置。

仅保存简易/高级模式的非敏感设置。小白模式 Base URL 可按产品决策选择不保存或单独保存最近地址，但 API Key、远端日志、Authorization 和原始响应绝不保存。当前实现要求 API Key 请求后清空。

### 14.2 存储键

```text
relay-cache-calculator:v1
```

存储结构必须包含版本号，后续升级时可以迁移或安全清除旧数据。

### 14.3 隐私原则

- 不保存 API Key；
- API Key 只保存在本次请求的浏览器内存中，只由浏览器直接发送到经校验的目标站固定只读路径；不经过本站 Pages Function，请求发出后立即清空；
- 小白模式 Base URL 会发送到本站 Pages Function；API Key 只离开浏览器前往目标站。界面、README 和页脚必须明确区分两条路径；
- Pages Function 的请求体严格只允许 `baseUrl`，不持久化请求内容，不接收 API Key、Authorization、Cookie 或上游日志原文；
- 简易/高级模式的计算参数和金额计算继续在浏览器本地完成；
- 不接入非必要分析脚本；
- 精确账单数据默认只保存在当前浏览器；
- 提供“清除本地数据”能力。

Cloudflare 作为托管平台可能按账户配置保留常规访问、安全和运行日志，产品隐私说明应避免承诺平台层面“绝无任何记录”。

## 15. 输入校验与异常处理

### 15.1 阻断性错误

- 单价小于 0；
- 倍率小于 0；
- 汇率小于等于 0；
- 缓存率小于 0% 或大于 100%；
- 输入比例与输出比例同时为 0；
- 预算小于 0；
- 精确用量 token 小于 0；
- 以总 token 为分母时，缓存率大于输入占比；
- 必填字段为空或无法解析。
- 小白模式 Base URL 不是 HTTPS 公网地址，或包含凭据、query、fragment、非允许端口/路径；
- 自动读取结果缺少完成当前计算必需的倍率或价格字段。

### 15.2 非阻断性警告

- 缓存读取价格高于普通输入价格；
- 模型倍率为 0，代表免费调用；
- 模型倍率异常高；
- 汇率与常见范围明显不符；
- 预设价格更新时间较早；
- 当前使用最终单价模式，倍率不会参与计算；
- 输出占比为 0 时，输出价格不会影响结果；
- 缓存命中率只作用于输入 token。
- 最近日志样本过少或时间跨度过短；
- 自动读取只获得部分端点，当前结果混合了远端数据与手填数据；
- `cache_ratio` 是计费倍率，不是缓存命中率。

### 15.3 特殊值处理

- 空字段：显示待输入状态；
- 成本为 0：预算容量显示“无法计算（单价为 0）”，不显示 Infinity；
- 无缓存成本为 0：节省比例显示“无法计算”；
- 预算为 0：可用 token 为 0；
- 极大 token 数：使用高精度计算并提供简写；
- 不允许页面出现 `NaN`、`undefined` 或科学计数法乱码。

## 16. 可访问性要求

- 所有输入框都有可见标签；
- 单位不能只依赖 placeholder；
- 数字输入设置合适的 `inputMode`；
- 滑块与数字输入使用可访问名称；
- 错误信息通过字段关系与输入框关联；
- 键盘可完成全部操作；
- 焦点样式清晰可见；
- 颜色对比度达到 WCAG AA；
- 结果变化可以使用节制的 `aria-live="polite"`；
- 避免每次键入都连续打断屏幕阅读器，可对播报做短暂防抖；
- 费用构成不能只依赖颜色，还应显示文字和数值。

## 17. 性能与兼容性

### 17.1 性能目标

- 首屏为纯静态资源；
- 输入后计算响应应接近即时；
- 不加载大型图表库；
- 不加载非必要图片；
- 模型价格表按需控制体积；
- 生产构建无明显控制台错误。

### 17.2 浏览器范围

优先支持当前稳定版本的：

- Chrome；
- Edge；
- Safari；
- Firefox；
- iOS Safari；
- Android Chrome。

## 18. 测试方案

### 18.1 单元测试

计算引擎必须覆盖：

| 编号 | 测试场景 | 预期结果 |
|---|---|---|
| U01 | 缓存率 0%，倍率 1 | 输入成本等于普通输入价格 |
| U02 | 缓存率 100% | 输入成本等于缓存读取价格 |
| U03 | 倍率从 1 变为 2 | 成本翻倍，预算 token 减半 |
| U04 | 缓存价等于普通输入价 | 缓存节省为 0 |
| U05 | 输出占比 0% | 混合成本等于有效输入成本 |
| U06 | 输出占比 100% | 混合成本等于输出成本 |
| U07 | 美元价格 | 正确应用人民币汇率 |
| U08 | 最终单价模式 | 不再应用模型和分组倍率 |
| U09 | 预算为 0 | 可用 token 为 0 |
| U10 | 单价为 0 | 不返回 Infinity |
| U11 | 总 token 缓存率大于输入占比 | 返回校验错误 |
| U12 | 标准示例 | 返回 11.616 元和约 860,881.54 token |
| U13 | 精确用量模式 | 各费用桶之和等于总费用 |
| U14 | 高精度小数 | 中间结果不因浮点误差明显偏移 |
| U15 | `cache_ratio` 与命中率同时存在 | 分别用于缓存单价和命中占比，不混用 |
| U16 | OpenAI 日志缓存口径 | `cached_tokens ÷ prompt_tokens` |
| U17 | Anthropic 拆分日志口径 | `cached_tokens ÷ (prompt_tokens + cached_tokens)` |
| U18 | 多端点部分失败 | 保留可用模型/分组并返回端点级警告 |
| U19 | API Key 请求路由 | 只随 `/api/log/token` 发送，不随公开配置请求发送 |
| U20 | 451 | 返回受限错误且不进行备用域名/代理重试 |
| U21 | SSRF 地址 | localhost、私网、IP 字面量、非法端口和重定向被拒绝 |
| U22 | 日志与错误脱敏 | 响应和异常中不出现 API Key 或 Authorization |

### 18.2 性质测试

在 `Pc <= Pi` 的前提下：

- 缓存率增加时成本不能增加；
- 倍率增加时成本线性增加；
- 预算可用 token 与成本成反比；
- 费用构成之和必须等于总成本；
- 缓存节省金额不能为负数。

如果 `Pc > Pi`，允许出现负节省，但必须显示警告。

### 18.3 组件测试

- 滑块和数字框同步；
- 模式切换显示正确字段；
- 最终单价模式隐藏或禁用倍率；
- 错误信息显示在正确字段附近；
- 重置恢复默认值；
- 复制结果内容正确；
- 本地设置可以恢复；
- 小白模式、简易模式、高级模式切换时状态互不覆盖；
- API Key 请求结束（成功、失败、取消）后输入框清空且不写入持久化；
- 自动读取部分成功时可选择模型/分组并手填缺失项；
- 页面明确区分计费缓存倍率和运行时缓存命中率。

### 18.4 端到端测试

- 完成一次“选择模型 → 输入倍率 → 输入缓存率 → 查看结果”的主流程；
- 完成混合 token 模式；
- 完成精确账单模式；
- 刷新页面后恢复设置；
- 桌面端无布局问题；
- 移动端无横向溢出；
- 仅使用键盘可以完成计算；
- 页面中不出现 `NaN`、`Infinity` 或未处理异常；
- 本地 `pnpm build` 后用 `pnpm dev:pages` 验证真实 Pages Function 路由；
- 小白模式完成“Base URL → 读取 → 选择模型/分组 → 查看结果”流程；
- 公开端点关闭、旧 One API、401/403、451、超时和未知 JSON 均能安全降级；
- 使用低权限临时 Key 的测试后，检查 DOM、URL、`localStorage`、控制台和网络错误信息中没有 Key；
- 回到简易/高级模式后，原设置与结果保持不变。

## 19. 开发阶段与工作量

### 阶段 0：口径确认与设计规格

预计：0.5 天

- 确认计价方式；
- 确认缓存率分母；
- 确认 MVP 模型预设范围；
- 完成主界面视觉概念；
- 固化标准示例和验收数据。

### 阶段 1：项目初始化与计算引擎

预计：0.5～1 天

- 初始化 React、TypeScript 和 Vite；
- 建立目录结构；
- 定义数据类型；
- 实现高精度计算引擎；
- 完成核心单元测试。

### 阶段 2：主界面开发

预计：1 天

- 实现计价参数区；
- 实现中转倍率区；
- 实现使用结构区；
- 实现实时结果区；
- 实现公式详情和费用构成。

### 阶段 3：预设、持久化与辅助功能

预计：0.5 天

- 接入本地模型价格表；
- 实现本地保存；
- 实现重置；
- 实现复制结果；
- 完成格式化和警告提示。

### 阶段 4：响应式、可访问性和 QA

预计：0.5～1 天

- 完成移动端布局；
- 完成键盘与屏幕阅读器基础适配；
- 执行单元、组件和端到端测试；
- 检查桌面及移动端实际渲染；
- 修复视觉和交互问题；
- 完成生产构建验证。

### 阶段 5：独立小白模式与 Pages Function

预计：1.5～3 天

- 建立独立小白模式状态与 UI，不改写简易/高级持久化数据；
- 实现 New API 兼容端点的标准化解析和部分成功策略；
- 实现 `/api/log/token` 最近日志缓存命中率聚合与协议口径识别；
- 实现 HTTPS 公网校验、DNS 预检、固定路径、超时、重定向和响应大小限制；
- 实现 API Key 单次内存生命周期与全链路脱敏；
- 增加 Function、解析器、组件、模式隔离和 E2E 测试；
- 用 `pnpm build` 后的 `pnpm dev:pages` 完成本地浏览器验收；
- 使用 Wrangler CLI 部署并验证静态页面与 `/api/relay/inspect`。

### 总工作量

原简易/高级 MVP 在无后端、模型预设数量有限的前提下约需 3～4 个工作日；新增小白模式和安全受限的 Pages Function 后，总体预计约 4.5～7 个工作日。实际工作量主要取决于需要兼容的私有 fork 数量和可获得的真实脱敏响应样例。

## 20. 交付物

- 可运行的前端项目源码；
- 模型价格预设文件；
- 计算引擎；
- 单元测试；
- 端到端测试；
- 响应式主页面；
- 构建产物；
- 独立小白模式与标准化客户端；
- Cloudflare Pages Function 及 `wrangler.jsonc`；
- New API 兼容端点、SSRF 防护和密钥脱敏测试；
- 使用说明；
- 本开发方案；
- 标准计算样例与验收记录。

## 21. 验收标准

### 21.1 功能验收

- 用户可以选择预设模型或填写自定义价格；
- 用户可以在不影响简易/高级模式的前提下进入独立小白模式；
- 小白模式可通过 Base URL 读取至少一个受控 New API 测试站的模型/分组/倍率，或明确显示端点级降级；
- 可选普通 API Key 只用于 `/api/log/token`，请求后清空且不持久化；
- 页面明确区分计费 `cache_ratio` 与运行时缓存命中率；
- HTTP 451 不绕过，旧 One API/关闭接口可以安全转为手填；
- 用户可以输入模型倍率和缓存率；
- 用户可以切换三种计算模式；
- 页面实时显示每 1M token 成本；
- 页面实时显示指定预算可用 token；
- 页面显示缓存节省金额和比例；
- 页面显示费用拆分；
- 页面可以展开查看实际代入公式；
- 设置可以在刷新后恢复；
- 重置和复制结果功能可用。

### 21.2 数值验收

使用第 6 章标准示例时，必须得到：

```text
每 1M 输入 token 成本：5.52 元
每 1M 混合 token 成本：11.616 元
10 元可用混合 token：约 860,881.54
无缓存成本：16.8 元
缓存节省：5.184 元
缓存节省比例：约 30.86%
```

### 21.3 质量验收

- 不显示错误的 `NaN` 或 `Infinity`；
- 计算中间值不提前舍入；
- 费用构成之和等于总费用；
- 桌面端和移动端无内容遮挡；
- 没有横向溢出；
- 键盘可操作；
- 生产构建通过；
- 自动化测试通过；
- 浏览器控制台无未处理错误；
- Function 只允许 HTTPS 公网目标与固定端点，私网/保留地址、重定向和超大响应被拒绝；
- 自动化测试确认 API Key 不出现在响应、错误、URL 和本地持久化；
- `pnpm build` 后 `pnpm dev:pages` 能同时运行静态站点与 Function；
- 生产发布使用 Wrangler CLI，线上 `/api/relay/inspect` 返回 Function JSON 而非静态 HTML。

## 22. 实施前需要最终确认的产品决策

以下内容不阻塞方案成立，但应在编码前固定：

1. 首批内置哪些模型；
2. 模型价格由谁维护；
3. 默认是否只展示输入 token 模式；
4. 混合模式是否默认使用 4:1 的输入输出比例；
5. 是否在 MVP 中加入缓存写入费用；
6. 是否在 MVP 中支持“缓存率占总 token”口径；
7. 是否需要可分享 URL；
8. 是否需要导出计算报告；
9. 最终产品名称及品牌色；
10. 是否保存最近使用的 Base URL（默认不保存）；
11. 是否将公网目标收紧为运营方审核过的域名 allowlist；
12. 日志聚合默认样本条数、最大时间跨度和最小可信样本数。

推荐的 MVP 决策如下：

- 默认进入“仅输入 token”模式；
- 混合模式初始比例为 4:1，但明确标注为可编辑示例；
- 缓存率默认以输入 token 为分母；
- 缓存写入放入精确账单模式；
- 模型预设采用本地 JSON；
- 暂不自动更新价格和汇率；
- 暂不加入账号、数据库或通用业务后端；仅保留受限读取用途的 Pages Function；
- 简易/高级模式的非敏感输入保存在本地浏览器；
- 小白模式 API Key 绝不保存，请求结束后清空；
- Base URL 默认不持久化；
- 自动读取失败时明确手填降级，不绕过站点限制。

## 23. 完成定义（Definition of Done）

当以下条件全部满足时，MVP 才视为完成：

- 需求范围内的三种计算模式全部可用；
- 标准计算示例结果完全一致；
- 关键边界值测试通过；
- 输入校验和异常提示完整；
- 本地保存、重置和复制结果正常；
- 桌面、手机布局均通过实际浏览器检查；
- 无明显可访问性阻断问题；
- 生产构建成功；
- 自动化测试全部通过；
- 计算公式与页面说明一致；
- 预设价格具有更新时间，示例价格不会被误认为官方当前价格；
- 小白模式与简易/高级模式的状态和持久化互不污染；
- 计费缓存倍率与运行时缓存命中率在数据模型、文案和公式中严格区分；
- 支持规定的 New API 兼容端点，并对私有 fork/旧 One API 部分成功或安全降级；
- API Key 单次内存生命周期、仅 `/api/log/token` 转发和脱敏测试全部通过；
- SSRF 防护、451 不绕过、超时/响应大小/重定向限制均有自动化或可复现验收；
- README 与界面隐私文案明确说明 Base URL/Key 会经同源 Pages Function，不再声称所有数据不上传；
- `wrangler.jsonc` 与 `functions/` 保持在项目根，Wrangler 本地与线上 Function 验证通过；
- 项目文档可以让其他开发人员独立启动、测试和继续维护。
