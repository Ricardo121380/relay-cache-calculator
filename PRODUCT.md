# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

使用 AI 中转站的开发者和普通用户。他们需要在选择模型、计价线路或中转站之前，把站点综合倍率、缓存读取折扣和真实缓存命中率换算成可比较的人民币成本。

## Product Purpose

把容易混淆的中转站计费参数转换成两类直接结论：每 1M token 的实际成本，以及固定预算可使用的 token 数量。成功意味着用户能快速理解结果口径，并能在简易、高级、小白和多站对比流程之间选择适合自己的输入方式。

## Positioning

计算器把缓存命中率与缓存读取倍率分开建模，并展示缓存后的实际等效倍率；小白模式还能读取站点公开配置，并在用户提供普通 API Key 时由浏览器直接读取近期日志，避免 API Key 经过本站服务器。对 Krill AI 可无密钥读取公开模型价格、独立计价线路、分渠道状态及最近 24 小时缓存率。

## Operating Context

用户通常同时参考中转站面板、模型价格、分组倍率和近期调用日志。结果需要适合桌面端仔细比较，也需要在手机上快速查询与复制。

## Capabilities and Constraints

- 简易模式、高级模式、小白模式和最多五站对比相互独立。
- 计算逻辑在浏览器内完成；小白模式的站点地址只用于读取公开数据，普通 API Key 仅由浏览器直连目标站固定只读接口且不保存。
- 目标站的 CORS、权限或地区限制可能阻止自动读取，此时必须提供清晰的手动输入降级路径。
- Krill 的计价线路与状态渠道是两个独立选择器；多项时不得自动猜测、配对或平均。
- 站点接口数据仅供估算，产品不对其真实性、准确性和时效性负责，结果以站点说明和实际账单为准。
- 部署目标是现有 Cloudflare Pages 项目 `relay-cache-calculator`，生产分支为 `main`。

## Brand Commitments

- 产品名称保持“中转站缓存成本计算器”。
- 界面采用整体 Liquid Glass 材料语言，参考 `liquidglassdesign.com`；玻璃是完整的功能层和光学层级，不使用漂浮白色胶囊或气泡来冒充液态效果。
- 语气直接、技术准确，不夸大自动读取能力。

## Evidence on Hand

- 计算、验证和格式化逻辑位于 `src/features/calculator/`。
- 小白模式的数据安全边界与日志聚合逻辑位于 `src/features/novice/` 和 `functions/`。
- 模型参考价格位于 `src/data/models.json`，仅作为参考数据，不应描述为官方实时价格。

## Product Principles

1. 先给结论，再解释倍率与公式。
2. 严格区分静态缓存价格倍率与动态缓存命中率。
3. 自动读取失败时说明原因和恢复方式，不隐藏手动路径。
4. 视觉表达不得遮挡数据、降低对比度或改变计算含义。
5. 涉及 API Key 的行为必须在界面中清楚说明其发送路径和保存状态。
6. 自动读取的数据必须标注来源、范围和读取时间；来源声明不能与错误或安全提醒混用。

## Accessibility & Inclusion

支持键盘操作、可见焦点、语义化表单、屏幕阅读器结果播报、明暗主题、减少动态效果和减少透明度偏好；核心信息不依赖颜色或透明效果单独传达。
